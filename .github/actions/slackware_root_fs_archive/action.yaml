name: Create Slackware Root Filesystem Archive
description: Create an archive of a root filesystem for a Slackware base image

runs:
  using: 'docker'
  image: 'Dockerfile'

branding:
  icon: 'package'
  color: 'blue'